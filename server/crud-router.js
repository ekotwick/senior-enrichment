// 'use strict';

// export default class CRUDRouter() {

// 	constructor(routerName){
// 		this.routerName = routerName;
// 		this.modelName = `${this.routerName[0].toUpperCase()}${this.routerName.slice(1)}`;
// 		this.invalidId = `Invalid ${routerName} id`;
// 		this.noInstances = `No ${routerName} found`;
// 	}

// 	param()

// }